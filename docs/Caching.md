# Caching

Each entity access or query results can be cached.

Two types of caching are supported;

## Cache Types

### Memory Cache

...

### Hybrid (Distributed) Cache

...

## Configuration

...

## Using Cache

Each entity (with id) and query results can be cached.

For default; Id cache always enabled for each entity. For change default cache behavior, see next sections ...

...

### With implementer

For default cache usage per entity; 

#### Concrete Entities

...

#### Dynamic Entities

...

### With per query

Each query can use `UseQueryCache` / `SkipQueryCache` methods



